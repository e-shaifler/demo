spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lol2
    username: liferay
    password: liferay
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging:
  level:
    com:
      example: debug
     